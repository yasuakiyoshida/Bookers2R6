<% if user_signed_in? %>
  <div class="search text-center">
    <%= form_with url: search_path, method: :get, local: true do |f| %>
      <!-- 検索入力フォーム -->
      <%= f.text_field 'search[value]' %>
      <!-- モデル(ユーザーか投稿か)を選択するプルダウンメニュー -->
      <%= f.select 'search[model]', options_for_select({ "User" => "user", "Book" => "book" }) %>
      <!-- 検索手法を選択するプルダウンメニュー -->
      <%= f.select 'search[how]', options_for_select({ "完全一致" => "match", "前方一致" => "forward", "後方一致" => "backward", "部分一致" => "partical" }) %>
      <%= f.submit '検索' %>
    <% end %>
  </div>
<% end %>
<!-- f.select ： セレクトボックスを用意 -->
<!-- options_for_select(要素の配列 or ハッシュ [, 選択された要素]) ： 選択肢生成 -->
<!-- ハッシュで、表示名(キー) => 参照したいモデル名or変数名(値) にする-->